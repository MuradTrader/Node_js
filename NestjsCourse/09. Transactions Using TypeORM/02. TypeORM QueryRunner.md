## 🏗️ **Архитектура транзакций в TypeORM**

### 🔧 **Query Runner - основной инструмент**

Автор объясняет, что для работы с транзакциями в TypeORM используется **Query Runner** (исполнитель запросов).

**Что такое Query Runner:**

- Это специальный класс в TypeORM
- Его экземпляр используется для выполнения транзакций
- Работает с **одним соединением** из пула соединений БД

### 🌊 **Connection Pool (Пул соединений)**

Автор вводит важное понятие:

**Пул соединений** - это набор заранее установленных соединений с базой данных, которые приложение может переиспользовать.

**Преимущества пула соединений:**

- Избегаем накладных расходов на установление нового соединения для каждого запроса
- Контролируемое количество одновременных соединений
- Эффективное управление ресурсами БД

### 📋 **Пошаговый процесс транзакции**

Автор описывает точную последовательность действий:

#### 1. **Создание Query Runner**

```typescript
// Создаем экземпляр Query Runner
const queryRunner = dataSource.createQueryRunner();
```

#### 2. **Установка соединения**

```typescript
// Получаем одно соединение из пула
await queryRunner.connect();
```

#### 3. **Начало транзакции**

```typescript
// Начинаем транзакционную сессию
await queryRunner.startTransaction();
```

#### 4. **Выполнение операций**

В этой фазе выполняются все CRUD-операции, которые должны быть частью транзакции.

#### 5. **Завершение транзакции**

**Два возможных сценария:**

**Успешное выполнение:**

```typescript
// Все операции прошли успешно - фиксируем изменения
await queryRunner.commitTransaction();
```

**Ошибка:**

```typescript
// Какая-то операция failed - откатываем все изменения
await queryRunner.rollbackTransaction();
```

#### 6. **Освобождение соединения**

```typescript
// Возвращаем соединение обратно в пул
await queryRunner.release();
```

### 🛡️ **Обработка ошибок с try-catch**

Автор подчеркивает важность правильной обработки ошибок:

```typescript
try {
  await queryRunner.startTransaction();
  // Выполняем операции...
  await queryRunner.commitTransaction();
} catch (error) {
  await queryRunner.rollbackTransaction();
  throw error; // Пробрасываем ошибку дальше
} finally {
  await queryRunner.release(); // Всегда освобождаем соединение
}
```

### 🔄 **Жизненный цикл транзакции**

1. **Connect** → Установка соединения
2. **Start Transaction** → Начало транзакции
3. **CRUD Operations** → Выполнение операций
4. **Commit/Rollback** → Фиксация или откат
5. **Release** → Освобождение соединения

### 🎯 **Ключевые моменты, которые подчеркивает автор:**

- **Одно соединение** на всю транзакцию
- **Обязательное освобождение** соединения (release)
- **Atomicity гарантируется** через commit/rollback
- **Try-catch блок** критически важен для обработки ошибок

Это фундаментальный паттерн, который мы будем применять в нашем NestJS приложении для обеспечения целостности данных! 🚀
