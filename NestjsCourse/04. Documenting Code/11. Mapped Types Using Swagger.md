### Общий контекст

Автор продолжает работу над документированием API, переходя от описания запросов к описанию ответов и созданию эффективных DTO для операций обновления с помощью mapped types.

---

### Детальное объяснение по разделам:

#### 1. Документирование ответов API

**Проблема:** В Swagger не отображается информация о том, какие ответы возвращает API.

**Решение - декораторы для контроллера:**

```typescript
import { ApiOperation, ApiResponse } from '@nestjs/swagger';

@Post()
@ApiOperation({ summary: 'Creates a new blog post' })
@ApiResponse({
  status: 201,
  description: 'The post has been successfully created.'
})
public createPost(@Body() createPostDto: CreatePostDto) {
  // логика метода
}
```

**Объяснение декораторов:**

- **`@ApiOperation()`** - описывает саму операцию (метод)

  - `summary` - краткое описание того, что делает метод
  - Также может содержать `description` для более подробного объяснения

- **`@ApiResponse()`** - документирует возможные HTTP ответы
  - `status` - HTTP статус код (201, 400, 500 и т.д.)
  - `description` - человеко-читаемое описание этого статуса
  - Можно добавлять несколько `@ApiResponse()` для разных статусов

**Результат в Swagger:**

- Над методом появляется описание "Creates a new blog post"
- В разделе Responses показывается, что при успехе вернется 201 статус

---

#### 2. Создание PATCH endpoint для обновления постов

**Зачем нужен PATCH:**

- **POST** - для создания новых ресурсов
- **PATCH** - для частичного обновления существующих ресурсов
- В PATCH передаются только те поля, которые нужно изменить

**Создание endpoint:**

```typescript
@Patch()
@ApiOperation({ summary: 'Updates an existing blog post' })
@ApiResponse({
  status: 200,
  description: 'The post has been successfully updated.'
})
public updatePost(@Body() patchPostDto: PatchPostDto) {
  console.log(patchPostDto);
  // здесь будет логика обновления
}
```

**Особенности PATCH:**

- Использует статус 200 (а не 201 как POST)
- Принимает только ID и те поля, которые нужно обновить

---

#### 3. Создание DTO для PATCH запроса с помощью Mapped Types

**Проблема:** Нам нужен DTO, который:

- Содержит все те же поля, что и `CreatePostDto`
- Но все поля (кроме ID) должны быть опциональными
- Не хочется дублировать весь код валидации и документации

**Решение - `PartialType`:**

```typescript
import { PartialType } from "@nestjs/swagger";
import { CreatePostDto } from "./create-post.dto";

export class PatchPostDto extends PartialType(CreatePostDto) {
  @ApiProperty({
    description: "The ID of the post that needs to be updated",
  })
  @IsInt()
  @IsNotEmpty()
  id: number;
}
```

**Что делает `PartialType`:**

- Берет все поля из `CreatePostDto`
- Делает их опциональными (добавляет `?` в TypeScript)
- **Сохраняет все декораторы валидации и Swagger документацию!**
- Автоматически применяет `@ApiPropertyOptional()` ко всем унаследованным полям

**Критически важный момент:**

- Нужно импортировать `PartialType` из `@nestjs/swagger` (а не из `@nestjs/mapped-types`)
- Только тогда Swagger подхватит всю документацию из базового DTO

---

#### 4. Структура PatchPostDto после применения PartialType

**Что получается:**

```typescript
// Автоматически генерируется PartialType:
class PatchPostDto {
  id: number; // ← обязательное поле (добавлено вручную)
  title?: string; // ← опциональное (было обязательным в CreatePostDto)
  postType?: PostType; // ← опциональное (было обязательным в CreatePostDto)
  slug?: string; // ← опциональное (было обязательным в CreatePostDto)
  status?: PostStatus; // ← опциональное (было обязательным в CreatePostDto)
  content?: string; // ← опциональное (и было опциональным)
  // ... и все остальные поля становятся опциональными
}
```

**Почему ID обязателен:**

- При создании поста (POST) ID еще не существует - его генерирует база данных
- При обновлении поста (PATCH) мы должны указать, какой именно пост обновляем
- ID - это идентификатор существующего ресурса

---

#### 5. Тестирование PATCH endpoint

**Пример валидного PATCH запроса:**

```json
{
  "id": 1,
  "title": "Updated title"
}
```

- ✅ Обновит только title у поста с ID=1
- ✅ Остальные поля останутся неизменными

**Пример валидного запроса с несколькими полями:**

```json
{
  "id": 1,
  "title": "Updated title",
  "status": "published"
}
```

**Пример невалидного запроса:**

```json
{
  "id": 1,
  "postType": "invalid_value" // ← нет в enum
}
```

- ❌ Вернет 400 Bad Request с сообщением об ошибке валидации

---

#### 6. Сравнение CreatePostDto и PatchPostDto в Swagger

**CreatePostDto (POST):**

- Все поля обязательные (кроме явно опциональных)
- Используется для создания нового ресурса
- Требует полного набора данных

**PatchPostDto (PATCH):**

- Только `id` обязательное поле
- Все остальные поля опциональные
- Используется для частичного обновления
- Можно отправить только те поля, которые нужно изменить

---

### Итог проделанной работы:

1. **Документированы ответы API** с помощью `@ApiOperation()` и `@ApiResponse()`

   - Пользователи API теперь видят, что ожидать в ответ
   - Понимают назначение каждого endpoint'а

2. **Создан PATCH endpoint** для обновления постов

   - Следует REST принципам
   - Позволяет эффективно обновлять ресурсы

3. **Применены Mapped Types** с `PartialType`

   - Исключено дублирование кода
   - Сохранена вся валидация и документация
   - Автоматическое преобразование обязательных полей в опциональные

4. **Обеспечена консистентность** между TypeScript и Swagger

   - Правильный импорт `PartialType` из `@nestjs/swagger`
   - Автоматическое наследование документации

5. **Протестирована функциональность**
   - Проверены валидные и невалидные запросы
   - Убедились, что валидация работает корректно

**Профессиональный результат:** Теперь ваш API имеет полную документацию как для запросов, так и для ответов, а код DTO эффективно переиспользуется без дублирования. Это уровень production-ready приложения! 🚀

Такой подход значительно упрощает поддержку кода и делает API понятным для других разработчиков.
