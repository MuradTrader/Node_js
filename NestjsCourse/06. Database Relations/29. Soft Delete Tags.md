## 🏗️ **Архитектура мягкого удаления**

### **Текущая конфигурация в Tag.entity.ts:**

```typescript
@Entity()
export class Tag {
  // ... другие свойства

  @DeleteDateColumn() // ← Декоратор для мягкого удаления
  deletedAt: Date; // Будет содержать timestamp удаления или NULL
}
```

## 🔄 **Разница между Hard Delete и Soft Delete**

### **Hard Delete (жёсткое удаление):**

```typescript
// Полное удаление записи из базы данных
await this.tagsRepository.delete(id);
// Результат: запись полностью удалена из таблицы
```

### **Soft Delete (мягкое удаление):**

```typescript
// Помечаем запись как удаленную, но оставляем в базе
await this.tagsRepository.softDelete(id);
// Результат: в deletedAt устанавливается timestamp
```

## 🛠️ **Создание Soft Delete endpoint**

### **Шаг 1: Новый метод в TagsService**

```typescript
public async softRemove(id: number) {
    await this.tagsRepository.softDelete(id); // ← Метод мягкого удаления
    return { deleted: true, id };
}
```

### **Шаг 2: Новый endpoint в TagsController**

```typescript
@Delete('soft-delete') // ← Дополнительный путь в URL
public softDelete(@Query("id", ParseIntPipe) id: number) {
    return this.tagsService.softRemove(id);
}
```

## 🌐 **Разница в HTTP запросах**

### **Hard Delete endpoint:**

```http
DELETE http://localhost:3000/tags?id=3
```

### **Soft Delete endpoint:**

```http
DELETE http://localhost:3000/tags/soft-delete?id=3
```

## 🗃️ **Что происходит в базе данных**

### **До мягкого удаления:**

```sql
-- Таблица tag:
id | name        | slug        | deletedAt
1  | JavaScript  | javascript  | NULL
3  | TypeScript  | typescript  | NULL  ← Активный тег

-- Junction table post_tags_tag:
postId | tagId
8      | 1
8      | 3    ← Связь с TypeScript
9      | 1
9      | 3    ← Связь с TypeScript
```

### **После мягкого удаления тега ID 3:**

```sql
-- Таблица tag:
id | name        | slug        | deletedAt
1  | JavaScript  | javascript  | NULL
3  | TypeScript  | typescript  | 2024-01-15 14:30:00  ← Помечен как удаленный!

-- Junction table post_tags_tag (БЕЗ ИЗМЕНЕНИЙ):
postId | tagId
8      | 1
8      | 3    ← Связь ОСТАЛАСЬ!
9      | 1
9      | 3    ← Связь ОСТАЛАСЬ!
```

## ⚡ **Поведение TypeORM при мягком удалении**

### **Автоматическое поведение:**

- **Запросы `find()`** автоматически исключают записи с `deletedAt IS NOT NULL`
- **Связи остаются** - junction table не изменяется
- **Данные сохраняются** - запись остается в базе для истории/аудита

### **Пример запросов после мягкого удаления:**

```typescript
// Этот запрос НЕ покажет мягко удаленные теги
const activeTags = await this.tagsRepository.find();
// Результат: только теги с deletedAt = NULL

// Чтобы включить мягко удаленные, нужно явно указать:
const allTags = await this.tagsRepository.find({
  withDeleted: true, // ← Включаем удаленные записи
});
```

## 🔧 **Техническая реализация**

### **Метод `softDelete()` vs `delete()`:**

```typescript
// Жёсткое удаление - SQL: DELETE FROM tag WHERE id = 3
await this.tagsRepository.delete(3);

// Мягкое удаление - SQL: UPDATE tag SET deletedAt = NOW() WHERE id = 3
await this.tagsRepository.softDelete(3);
```

### **Восстановление мягко удаленных записей:**

```typescript
// Можно восстановить запись
await this.tagsRepository.restore(3);
// SQL: UPDATE tag SET deletedAt = NULL WHERE id = 3
```

## 🎯 **Практическая демонстрация автора**

### **Подготовка данных:**

1. Создаем тег TypeScript (ID 3)
2. Привязываем его к постам 8 и 9
3. Выполняем мягкое удаление

### **Результат:**

- ✅ **Тег ID 3** остается в таблице `tag` с заполненным `deletedAt`
- ✅ **Связи в junction table** сохраняются неизменными
- ✅ **Посты 8 и 9** продолжают иметь связь с тегом ID 3

## ⚠️ **Важные особенности мягкого удаления**

### **Проблема "призрачных" связей:**

```typescript
// Пост все еще имеет связь с мягко удаленным тегом
const post = await this.postsRepository.findOne({
  where: { id: 8 },
  relations: ["tags"],
});

console.log(post.tags);
// Покажет: [JavaScript, TypeScript]
// Но TypeScript мягко удален!
```

### **Решение - фильтрация в запросах:**

```typescript
// Нужно явно фильтровать мягко удаленные теги
const post = await this.postsRepository.findOne({
  where: { id: 8 },
  relations: ["tags"],
  relationLoadStrategy: "query", // Можно добавить условия к relations
});

// Или фильтровать на уровне сервиса
post.tags = post.tags.filter((tag) => tag.deletedAt === null);
```

## 💡 **Преимущества мягкого удаления**

### **1. Сохранение истории:**

- Можно анализировать какие теги использовались
- Восстановление данных при ошибке
- Аудиторские требования

### **2. Целостность связей:**

- Связи в junction table не нарушаются
- Исторические данные остаются согласованными
- Нет "битых" ссылок

### **3. Гибкость:**

- Можно сделать "корзину" для удаленных элементов
- Разные уровни удаления (архивация vs полное удаление)

## 🚨 **Проблемы и ограничения**

### **Производительность:**

```typescript
// Со временем таблица растет из-за "удаленных" записей
// Нужно периодически очищать старые удаленные записи
```

### **Сложность запросов:**

```typescript
// Все запросы должны учитывать статус удаления
// Нужно постоянно использовать withDeleted: true/false
```

## 🔄 **Сравнение подходов к удалению**

| Аспект                 | Hard Delete             | Soft Delete                 |
| ---------------------- | ----------------------- | --------------------------- |
| **Данные**             | Полностью удалены       | Сохраняются с меткой        |
| **Связи**              | Удаляются автоматически | Сохраняются                 |
| **Производительность** | Лучше                   | Хуже (данные накапливаются) |
| **Восстановление**     | Невозможно              | Возможно                    |
| **Аудит**              | Нет истории             | Полная история              |

## 🛡️ **Лучшие практики**

### **Для большинства случаев:**

```typescript
// Использовать мягкое удаление для:
// - Пользовательских данных
// - Контента (посты, теги, категории)
// - Любых данных, которые могут понадобиться для истории

// Использовать жесткое удаление для:
// - Временных данных
// - Кэшированных значений
// - Конфиденциальных данных (по требованию)
```

### **Архитектура API:**

```typescript
// Разные endpoints для разных типов удаления
@Delete('hard')      // Жесткое удаление
@Delete('soft')      // Мягкое удаление
@Delete('archive')   // Архивация
```

## 🎯 **Ключевые выводы**

1. **Мягкое удаление** помечает записи как удаленные, но сохраняет их в БД
2. **`@DeleteDateColumn()`** создает столбец для отслеживания удаления
3. **`softDelete()`** метод устанавливает timestamp в `deletedAt`
4. **Связи не затрагиваются** при мягком удалении
5. **TypeORM автоматически фильтрует** мягко удаленные записи в обычных запросах
6. **Нужно создавать отдельные endpoints** для разных типов удаления

Теперь вы понимаете разницу между жестким и мягким удалением и можете выбирать подходящий подход для разных сценариев в вашем приложении! 🚀
