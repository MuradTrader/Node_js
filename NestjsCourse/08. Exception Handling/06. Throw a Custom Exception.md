## 🎯 **ОСНОВНАЯ ЦЕЛЬ УРОКА**

Автор показывает **как создавать собственные (кастомные) исключения** в NestJS, когда встроенных исключений недостаточно для специфических сценариев.

## 🔧 **ШАГ 1: ПОНИМАНИЕ НЕОБХОДИМОСТИ КАСТОМНЫХ ИСКЛЮЧЕНИЙ**

### **Текущая ситуация с встроенными исключениями:**

```typescript
// Встроенные исключения покрывают 95% случаев:
throw new BadRequestException(); // 400
throw new NotFoundException(); // 404
throw new ConflictException(); // 409
// ... и другие

// НО: что если нужен специфический статус или структура?
```

### **Проблема, которую решает автор:**

> "What if there is a corner case and you want to throw an exception which is not built into Nestjs common package?"

**Пример сценария от автора:**

- Endpoint `/users` перемещен навсегда
- Нужно вернуть статус `301 Moved Permanently`
- Встроенного `MovedPermanentlyException` не существует

## 🔧 **ШАГ 2: СОЗДАНИЕ КАСТОМНОГО ИСКЛЮЧЕНИЯ С ПОМОЩЬЮ `HttpException`**

### **Базовый синтаксис:**

```typescript
import { HttpException, HttpStatus } from '@nestjs/common';

throw new HttpException(
  response: string | object,  // Тело ответа
  status: number,             // HTTP статус код
  options?: { description?: string; cause?: any }  // Опциональные параметры
);
```

### **Конкретная реализация автора:**

```typescript
public findAll(
  getUserParamDto: GetUsersParamDto,
  limit: number,
  page: number
) {
  throw new HttpException(
    {
      // 🎯 ПЕРВЫЙ АРГУМЕНТ: тело ответа (отправляется клиенту)
      error: 'The API endpoint does not exist',
      fileName: 'users.service.ts',     // ⚠️ Опасно - раскрывает структуру!
      lineNumber: 88,                   // ⚠️ Опасно для продакшена!
    },
    // 🎯 ВТОРОЙ АРГУМЕНТ: HTTP статус код
    HttpStatus.MOVED_PERMANENTLY,       // 301
    {
      // 🎯 ТРЕТИЙ АРГУМЕНТ: опции (НЕ отправляются клиенту)
      description: 'Occurred because the API endpoint was permanently moved',
      cause: new Error('Endpoint deprecated'),  // Причина для логирования
    }
  );
}
```

## 🔍 **ШАГ 3: ДЕТАЛЬНЫЙ РАЗБОР КОМПОНЕНТОВ**

### **1. `HttpStatus` - перечисление статус-кодов**

**Что это такое:**

```typescript
// Файл: @nestjs/common/enums/http-status.enum.ts
export enum HttpStatus {
  CONTINUE = 100,
  SWITCHING_PROTOCOLS = 101,
  OK = 200,
  CREATED = 201,
  // ...
  MOVED_PERMANENTLY = 301, // ← Использует автор
  FOUND = 302,
  // ...
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  // ...
  INTERNAL_SERVER_ERROR = 500,
}
```

**Преимущества использования `HttpStatus`:**

```typescript
// ❌ ПЛОХО - магические числа
throw new HttpException(response, 301);

// ✅ ХОРОШО - семантические константы
throw new HttpException(response, HttpStatus.MOVED_PERMANENTLY);
```

### **2. Структура аргументов `HttpException`**

**Первый аргумент - `response` (отправляется клиенту):**

```typescript
// Вариант 1: строка (простое сообщение)
throw new HttpException("Endpoint moved", HttpStatus.MOVED_PERMANENTLY);

// Вариант 2: объект (расширенная информация) - использует автор
throw new HttpException(
  {
    error: "The API endpoint does not exist",
    fileName: "users.service.ts", // ⚠️ Информация о файловой системе
    lineNumber: 88, // ⚠️ Детали реализации
  },
  HttpStatus.MOVED_PERMANENTLY
);
```

**Второй аргумент - `status` (числовой код):**

```typescript
// Всегда используйте HttpStatus для ясности
HttpStatus.MOVED_PERMANENTLY; // 301
HttpStatus.BAD_REQUEST; // 400
HttpStatus.NOT_FOUND; // 404
HttpStatus.INTERNAL_SERVER_ERROR; // 500
```

**Третий аргумент - `options` (для логирования, НЕ для клиента):**

```typescript
{
  description: 'Occurred because the API endpoint was permanently moved', // Для логов
  cause: new Error('Endpoint deprecated'), // Цепочка ошибок
  // Любые другие поля для внутреннего использования
}
```

## 🚨 **ШАГ 4: БЕЗОПАСНОСТЬ И ПРЕДУПРЕЖДЕНИЯ АВТОРА**

### **Опасные практики, которые автор демонстрирует (но предупреждает):**

```typescript
// ❌ ОПАСНО ДЛЯ ПРОДАКШЕНА:
{
  fileName: 'users.service.ts',   // Раскрывает структуру проекта
  lineNumber: 88,                 // Раскрывает детали реализации
}

// ✅ БЕЗОПАСНАЯ АЛЬТЕРНАТИВА:
{
  error: 'Endpoint moved permanently',
  newEndpoint: '/api/v2/users',    // Полезная информация для клиента
  documentation: 'https://api.example.com/migration-guide'
}
```

**Почему это опасно:**

- 🎯 **Информация для хакеров** - структура файлов, технологический стек
- 🔍 **Упрощение атак** - знание внутренней архитектуры
- 📝 **Нарушение абстракции** - клиенту не нужно знать про файлы и строки

## 🧪 **ШАГ 5: ТЕСТИРОВАНИЕ КАСТОМНОГО ИСКЛЮЧЕНИЯ**

### **HTTP запрос для тестирования:**

```http
GET http://localhost:3000/users/
```

### **Ожидаемый ответ:**

```json
{
  "statusCode": 301,
  "message": {
    "error": "The API endpoint does not exist",
    "fileName": "users.service.ts",
    "lineNumber": 88
  },
  "error": "Moved Permanently"
}
```

**Что важно заметить:**

- ✅ **Статус 301** корректно установлен
- ✅ **Тело ответа** содержит кастомные поля
- ❌ **Опции (третий аргумент)** НЕ показываются клиенту
- ⚠️ **Детали реализации** раскрыты (опасно для production)

## 🔧 **ШАГ 6: АЛЬТЕРНАТИВНЫЙ ПОДХОД - СОЗДАНИЕ КЛАССА ИСКЛЮЧЕНИЯ**

### **Более профессиональный способ (автор упоминает):**

```typescript
// Создание кастомного класса исключения
import { HttpException, HttpStatus } from "@nestjs/common";

export class MovedPermanentlyException extends HttpException {
  constructor(newLocation: string, message?: string) {
    super(
      {
        statusCode: HttpStatus.MOVED_PERMANENTLY,
        error: message || "Resource moved permanently",
        newLocation: newLocation, // ✅ Полезная информация
        documentation: "https://api.example.com/migration-guide",
      },
      HttpStatus.MOVED_PERMANENTLY
    );
  }
}

// Использование:
throw new MovedPermanentlyException("/api/v2/users", "Endpoint deprecated");
```

**Преимущества этого подхода:**

- ♻️ **Переиспользование** в разных местах приложения
- 🎯 **Типизация** и предсказуемость
- 🔒 **Безопасность** - контролируемая структура ответа
- 📚 **Документирование** через код

## 💡 **ШАГ 7: РЕАЛЬНЫЕ СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ КАСТОМНЫХ ИСКЛЮЧЕНИЙ**

### **1. Бизнес-логика приложения:**

```typescript
// Для специфичных бизнес-ошибок
throw new HttpException(
  {
    error: "Insufficient funds",
    requiredAmount: 100,
    currentBalance: 50,
    transactionId: "tx_123456",
  },
  HttpStatus.UNPROCESSABLE_ENTITY // 422
);
```

### **2. Кастомная валидация:**

```typescript
// Когда встроенных валидаций недостаточно
throw new HttpException(
  {
    error: "Password does not meet security policy",
    requirements: {
      minLength: 8,
      requireNumbers: true,
      requireSpecialChars: true,
    },
    suggestion: "Use at least 8 characters with numbers and symbols",
  },
  HttpStatus.BAD_REQUEST
);
```

### **3. Rate limiting:**

```typescript
// Ограничение запросов
throw new HttpException(
  {
    error: "Rate limit exceeded",
    limit: 100,
    resetTime: "2024-01-15T12:00:00Z",
    retryAfter: 3600, // seconds
  },
  HttpStatus.TOO_MANY_REQUESTS // 429
);
```

## 🎯 **ШАГ 8: ПРОФЕССИОНАЛЬНЫЕ РЕКОМЕНДАЦИИ**

### **Когда использовать кастомные исключения:**

| Сценарий                         | Рекомендация                      |
| -------------------------------- | --------------------------------- |
| **Стандартные HTTP ошибки**      | ✅ Встроенные исключения          |
| **Специфичные бизнес-ошибки**    | ✅ Кастомные исключения           |
| **Нестандартные статус-коды**    | ✅ `HttpException` с `HttpStatus` |
| **Расширенная структура ошибок** | ✅ Кастомные классы исключений    |

### **Структура хорошего кастомного исключения:**

```typescript
// Идеальная структура ответа:
{
  "statusCode": 301,
  "message": "Resource moved permanently",
  "error": "Moved Permanently",
  "newLocation": "/api/v2/resource",      // ✅ Полезно для клиента
  "documentation": "https://example.com/docs/migration", // ✅ Помощь
  "deprecationDate": "2024-06-01"         // ✅ Планирование
}
```

## 🔮 **ШАГ 9: ЧТО ДАЛЬШЕ**

### **Что автор упоминает о будущем:**

> "Use this only when existing exceptions are not sufficient."

**Рекомендации по использованию:**

1. **Сначала проверьте встроенные исключения** - они покрывают большинство случаев
2. **Для нестандартных сценариев** используйте `HttpException`
3. **Для частого переиспользования** создавайте кастомные классы
4. **Всегда думайте о безопасности** - не раскрывайте внутреннюю структуру

### **Потенциальные улучшения:**

- Создание `CustomExceptionFilter` для глобальной обработки
- Интеграция с системой логирования
- Стандартизация формата ошибок для всего API

## 💡 **КЛЮЧЕВЫЕ ВЫВОДЫ**

1. **`HttpException` - базовый класс** для создания кастомных исключений
2. **`HttpStatus` enum** содержит все стандартные HTTP статус-коды
3. **Третий аргумент `options`** не отправляется клиенту - только для логирования
4. **Безопасность важна** - не раскрывайте детали реализации
5. **Создавайте кастомные классы** для частого переиспользования

Этот урок показывает мощный механизм NestJS для создания гибкой и информативной системы ошибок! 🚀
