## 📋 **Обзор процесса аутентификации**

Автор описывает **полный цикл** password-based аутентификации, который состоит из нескольких ключевых этапов:

### 🔒 **1. Hashing & Salting (Хеширование и "Соление")**

**Проблема:** Хранение паролей в чистом тексте - это катастрофа безопасности.

**Почему это плохо:**

- **Доступ разработчиков** - все кто имеет доступ к БД видят пароли
- **Утечки данных** - хакеры получают пароли "как есть"
- **Повторное использование** - пользователи используют одни пароли на многих сервисах

**Решение:**

- **Хеширование** - преобразование пароля в необратимую строку фиксированной длины
- **"Соление"** - добавление случайных данных к паролю перед хешированием

```typescript
// Пример того, что будет в курсе (не реализация!)
// Пароль: "myPassword123"
// Соль: "randomSalt123"
// Хеш: "a1b2c3d4e5f6..." (нельзя обратно получить пароль)
```

### 🔑 **2. Аутентификация пользователя**

**Процесс входа:**

1. Пользователь вводит email и пароль
2. Система находит пользователя по email
3. Берет введенный пароль + соль из БД → вычисляет хеш
4. Сравнивает вычисленный хеш с сохраненным хешем
5. Если совпадают - пользователь аутентифицирован

### 🎫 **3. JWT Tokens (JSON Web Tokens)**

**После успешной аутентификации:**

- Система выдает **JWT токен**
- Это как цифровой паспорт пользователя
- Токен содержит информацию о пользователе (ID, email и т.д.)

**Структура JWT:**

```
header.payload.signature
```

- **Header** - алгоритм подписи
- **Payload** - данные пользователя
- **Signature** - цифровая подпись для проверки подлинности

### 🛡️ **4. Авторизация запросов**

**Для защищенных endpoints:**

1. Пользователь отправляет запрос с JWT в заголовках
2. Сервер проверяет:
   - **Подлинность подписи** - не изменяли ли токен
   - **Срок действия** - не истек ли токен
   - **Данные пользователя** - извлекает ID, права доступа
3. Если все ок - разрешает доступ к endpoint

## 🔄 **Полный поток данных:**

```
Регистрация → [Пароль] → Хеширование+Соль → Сохранение в БД
    ↓
Вход → [Email+Пароль] → Проверка хеша → Выдача JWT
    ↓
Запросы → [JWT в заголовке] → Проверка токена → Доступ к API
```

## 🎯 **Что будет в этом разделе курса:**

Автор обещает покрыть:

- ✅ **Теорию** хеширования и "соления"
- ✅ **Реализацию** безопасного хранения паролей
- ✅ **JWT** - создание и верификацию токенов
- ✅ **Middleware/Guards** для защиты endpoints
- ✅ **Полный workflow** от регистрации до доступа к API

Это фундаментальная тема для любого современного веб-приложения! Автор правильно начинает с общего обзора, чтобы вы понимали "общую картину" перед погружением в технические детали.
